首先下載原始碼，並且切換到當前最新版本 ( 當前版本 : v2.14.0 )。

```
$ git clone
$ git checkout -b v2.14.0 2.14.0
```

安裝編譯所需要的套件。

``` shell
sudo apt install git python3-distutils gcc g++ make file wget gawk diffstat bzip2 cpio chrpath zstd lz4 bzip2

