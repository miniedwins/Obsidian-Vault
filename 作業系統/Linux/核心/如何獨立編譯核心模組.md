編譯模組前，需要先將核心編譯完成。

範例 : 編譯 NVMe 模組

```
$ make -C . M=drivers/nvme
```

如果當前模組被使用，需要先將模組卸載

```
$ sudo modprobe "your_module_name"
```

複製模組檔案

```
$ sudo cp -a drivers/nvme/* /lib/modules/$(uname -r)/kernel/drivers/nvme/
```

更新核心模組相依性

```
$ dempod
```

掛載核心模組

```
$ sudo modprobe nvme
```