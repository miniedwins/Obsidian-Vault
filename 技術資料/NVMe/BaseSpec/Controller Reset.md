NVMe 規範中說明只要將 `CC.EN=1` 設定成 `CC.EN=0`，即是觸發 `Controller Reset`。

* 專門針對 NVMe 控製器本身的復位，當執行復位時，控製器會重啟，但不會重設某些持久屬性。
* 控製器復位不會影響與傳輸相關的設定。

**控製器級復位 ( Controller Level Reset, CLR )** 
* 當執行控製器級復位時，復位操作會影響整個控製器及其相關功能。
* 控製器級復位會根據相關的**NVMe傳輸繫結規範**對控製器屬性進行重設。