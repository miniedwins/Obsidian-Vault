
- MBR  
	- `Shadowing MBR` 所映射的虛擬的 LBA 位址
		- 可以從 Locking SP MBR Table 獲得，預設容量應該是 128MB。
		- 若是解除 `Shadowing MBR`，MBR Table 的大小會不會改變 ?

- Locking SP
	- 改變 Locking Range 範圍，例如 : LBA=0-1000，變成 0-799 範圍
		- 原先的 LBA=800-100，會變成什麼狀態 ? "Global Range" 
	- 解除 Locking Range 範圍，資料是否被保留 ? 
		- 目前測試的結果會被保留，但是SPEC未找到相關說明。