- **錯誤注入（Error Injection）的精準度**：如何在腳本中動態觸發特定的傳輸層錯誤（Transport Layer Error）或逾時（Timeout），並精確記錄 SSD 的恢復時間？
    
- **電力分析與控制**：測試腳本如何與系統的 Power 模組聯動，在執行特定指令（如 Sanitize）的過程中模擬異常斷電（Dirty Power Cycle）？
    
- **Sideband 訊號監控**：針對 NVMe-MI 測試，如何透過腳本同步監控 SMBus/I2C 的訊號狀態與帶內（In-band）指令的關聯性？

- **PCIe 暫存器** : 可以在介面上修改暫存器內容或是顯示 ?

- **UI命令** : 是否可以在介面上執行NVMe命令，例如: NVMe-MI, TCG OPAL, SMBUs 命令