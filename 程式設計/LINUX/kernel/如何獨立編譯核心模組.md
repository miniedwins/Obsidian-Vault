獨立安裝模組需要先將核心編譯完成

範例 : 編譯 NVMe 模組

```
$ make -C . M=drivers/nvme
```

如果當前模組被使用，需要先將模組卸載

```
# 卸載模組
$ sudo modprobe "your_module_name"
```

複製模組檔案

```
$ sudo cp drivers/nvme/* /lib/$(uname -r)/nvme/
```

更新核心模組相依性

```
$ dempod
```

掛載核心模組

```
$
```