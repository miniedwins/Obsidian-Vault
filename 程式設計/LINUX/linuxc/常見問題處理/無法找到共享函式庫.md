問題描述 :  
cannot open shared object file : No such file or directory

執行行程式時，遇到像下列這種錯誤：   

```
./main_dynamic: error while loading shared libraries: libsum.so.1: cannot open shared object file: No such file or directory
```

解決方法 :  
- `/etc/ld.so.conf` 中加入 `/usr/local/lib` 這一行，可以解決此問題。
- 存檔後，執行 `ldconfig` 更新一下才會生效。